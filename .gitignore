# Backup files with quotes or special characters
todo.ai ''
todo.ai ''

# RELEASE_SUMMARY.md should only be in release/ directory
# (Allow it in release/ but ignore in root)
RELEASE_SUMMARY.md
!release/RELEASE_SUMMARY.md

# macOS system files
.DS_Store
.AppleDouble
.LSOverride

# Editor backup files
*~
*.swp
*.swo
*#

# Auto-generated end-user cursor rules (created by init_cursor_rules())
# These are generated on install/update and don't need to be committed
.cursor/rules/todo.ai-*.mdc

# Backup files created by mode switching and rollback operations
.todo.ai/backups/


# Backup files created by various tools (sed, etc.)
*.bak
*.bak*
release/.prepare_state
