# Backup files with quotes or special characters
todo.ai ''
todo.ai ''

# RELEASE_SUMMARY.md should only be in release/ directory
# (Allow it in release/ but ignore in root)
RELEASE_SUMMARY.md
!release/RELEASE_SUMMARY.md

# macOS system files
.DS_Store
.AppleDouble
.LSOverride

# Editor backup files
*~
*.swp
*.swo
*#

# Auto-generated end-user cursor rules (created by init_cursor_rules())
# These are generated on install/update and don't need to be committed
.cursor/rules/ai-todo-*.mdc
.cursor/rules/todo.ai-*.mdc

# Legacy data directory (shell script creates .todo.ai/, Python uses .ai-todo/)
# Only ignore the old naming to prevent accidental commits from shell tests
/.todo.ai/

# Backup files created by mode switching and rollback operations
.ai-todo/backups/
.todo.ai/backups/


# Backup files created by various tools (sed, etc.)
*.bak
*.bak*
release/.prepare_state

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Local audit log (persists across checkouts)
.ai-todo/state/audit.log
.todo.ai/state/audit.log
